You are a Senior Product Manager, UX Designer, and Full-Stack Engineer helping refine an existing travel app prototype.

Important Instruction

Do NOT redesign the application from scratch.

Instead:

edit the existing prototype

improve UX clarity

improve flow logic

refine copy

adjust component placement

improve interaction design

Maintain the existing design language and layout where possible.

App Overview

Current working name: Wandr

However, trips generated by the app are typically 1–4 days (micro-trips).

Suggest alternative app name options that better reflect:

short trips

quick itineraries

spontaneous travel

curated escapes

Examples of themes:

weekend trips

micro-cations

quick escapes

Do not change the app name yet — only suggest alternatives.

Core UX Flows

Ensure the following user flows work logically.

Flow 1 — Solo Trip

Home Page
→ Enter destination
→ Trip Preferences Quiz
→ Generated Itinerary

Actions available:

Save
Share
Invite Others
Regenerate

Flow 2A — Group Trip (Started from Solo Trip)

User first creates a solo itinerary.

Then selects:

Invite Others

Options:

A. Share Link
B. View Group Results

Group results page actions:

Save
Share
Regenerate

Flow 2B — Group Trip Started from Home Page

Home Page
→ Toggle Group Mode
→ Enter destination
→ Trip Preferences Quiz

The first user submission generates the initial itinerary.

As additional group members submit preferences:

the itinerary dynamically updates

group preferences influence the final result.

Flow 3 — Inspiration Entry Point

If the user clicks an inspiration destination tile on the home page:

Destination Tile
→ Destination automatically fills the input field
→ Quiz
→ Itinerary

Actions:

Save
Share
Invite Others
Regenerate

Homepage Improvements
Solo / Group Toggle

Move the Solo / Group toggle to the left side of the destination input field.

Goal:

Prevent it from visually competing with the primary Curate CTA button.

CTA Hierarchy

The Curate / Generate button should remain the primary visual action.

Ensure the toggle:

looks different from a CTA

visually reads as a mode selector, not a button.

MVP Geography Scope

Since the MVP focuses on Canada, update example destinations.

Use sample inspiration cities such as:

Vancouver
Toronto
Montreal
Quebec City
Calgary
Victoria
Banff

These should appear in:

placeholder suggestions

inspiration cards

Quiz Improvements
Q1 — Trip Length

Currently includes a calendar field.

Change order to:

1️⃣ Number of days first
2️⃣ Optional calendar input after

This reduces friction.

Q3 — Group Question

If the Solo / Group toggle already exists on the Home Page, evaluate whether this question is redundant.

If redundant:

remove it
OR

repurpose the question.

Optional Questions

All questions after Q1 should be optional.

Provide a Skip option

, not visual redesign.